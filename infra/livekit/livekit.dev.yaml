port: 7880
log_level: info

rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false

keys:
  devkey: wapi-local-only-secret-32-characters

turn:
  enabled: true
  udp_port: 3478

room:
  empty_timeout: 300
  departure_timeout: 20

# En production, l'URL doit viser la fonction livekitWebhook et la clé doit
# correspondre à WAPI_LIVEKIT_API_KEY. Le profil local reste sans webhook.
