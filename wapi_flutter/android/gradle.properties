org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# The pool renderer is generated from the shared native game module. Gradle's
# source-set Provider guard is disabled only for this explicitly declared,
# task-dependent generated directory.
android.sourceset.disallowProvider=false
# Download the Google barcode model on first QR use instead of shipping the
# full ML Kit model in every WAPI installation.
dev.steenbakker.mobile_scanner.useUnbundled=true
